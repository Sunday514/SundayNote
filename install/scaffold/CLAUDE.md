# Claude Code Instructions

@AGENTS.md

## Claude Code Adapter

This vault uses `AGENTS.md` as the canonical agent instructions.

When working in this vault:

- Follow `AGENTS.md` as the source of truth.
- Treat references to "agent" as applying to Claude Code.
- Treat this vault primarily as a documentation-first knowledge base. When working under `SundayNoteAgent/`, follow `SundayNoteAgent/AGENTS.md` for tool-layer development and run basic checks when editing scripts or configuration.
- Do not edit personal notes, private writing, templates, or ignored runtime contents unless explicitly asked.
