# Agent 开发规则

维护可迁移、低复杂度、服务真实使用流程的 Agent 工具层。

## 范围与职责

- 修改本仓库的规则、Skills、脚本、模板和安装导出源文件。父 vault 的托管副本通过安装器更新。
- 个人正文、个人模板、设备信息、凭据和本地运行状态留在父 vault，不写入本仓库。
- Skills 负责语义工作流，脚本负责确定性操作，安装器和 scaffold 负责导出；保持源码真值唯一。
- README 提供入口，安装文档说明使用，scaffold 定义安装后的规则；同一约定放在职责最近的位置。

## 修改

- 修改前明确结果、范围和验证方式；只做完成当前目标所需的最小改动，无关问题单独记录。
- 规则和 Skill 按可执行指令编写，删除重复要求、默认能力复述和失效流程。直接描述当前行为，省略历史对照、包装句和无内容标题。
- Agent 文档及 Skill description 使用中文，保留 Raw、Routine、Wiki、Journal、Schema、index、log 等约定名词。SKILL.md 保持简洁，frontmatter 只放 name 和 description。
- 保持能力可复用，不绑定单个知识库案例。模板只保存稳定、通用的结构。
- 自动化使用固定 vault 布局，不增加层目录配置或本机绝对路径。

## 验证与交付

- 脚本、安装器和配置修改运行匹配的基本检查；涉及安装、导出、Obsidian 或布局时验证受影响的用户流程。
- 自动回归使用脱敏 fixture 和临时目录。父 vault 仅作为明确授权的临时集成实例，不依赖具体个人内容，也不留下长期测试产物。
- 报告实际结果、验证和未覆盖范围；检查通过后，只因新增改动、失败或未解决疑点扩大验证。
- 临时文件、运行明细和一次性布局留在临时目录，不进入长期契约。
