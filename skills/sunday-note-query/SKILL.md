---
name: sunday-note-query
description: 使用已有个人知识库上下文回答、判断、规划、推荐或讨论。用于用户询问个人记录、知识库内容、历史决策、已有项目、个人偏好、长期目标、近期计划、推荐偏好、工作流、规则文档、过往方案，或开放问题的答案可能被既有个人上下文改变，例如“适不适合我”“下一步怎么做”“推荐什么”“之前怎么定的”“按我的记录来看”。优先最少证据检索；不要用于通用知识、普通技术问答、纯外部事实查询、当前对话已足够回答的问题、资料摄取或知识库维护。
---

# Query

## 工作流

1. 从用户问题中提取 3-8 个关键词，优先保留用户对话中给出的名称、日期、项目线索、术语和短语；去掉泛词和任务动词。
2. 运行本 skill 的 `scripts/query_search.py <关键词...>` 获取候选文件列表。
3. 根据问题意图选择匹配度最高的候选作为证据文件：Wiki 看 `topic` / `keywords`，Routine 看日期、项目线索和命中词；证据文件不超过 3 个。
4. 使用 Wiki 作为证据文件时，记录本轮用到的 Wiki 路径；回答末尾固定给出 header 更新建议：`last_queried` 写当天，`query_count` 加 1；不要自动写入。
5. 读取证据文件，并以其内容作为证据回答问题；可使用 `%% agent ... %%` 注释块辅助理解，但回答用户时不暴露 agent-only 块，除非用户询问维护状态；不要粘贴大段搜索结果或原文。
6. 问题回答后，如产生新的稳定判断、项目状态变化、兴趣方向、近期计划、推荐偏好或高价值总结，给出写回建议，并说明写回时需要更新目标 Wiki 的 `last_updated` 和 `update_count`；不直接写回。
