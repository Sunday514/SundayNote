---
name: sunday-note-query
description: 回答需要从个人知识库检索个人记录、历史决策、项目上下文、目标、计划或既有知识时使用。
---

# Query

1. 需要了解个人长期项目、历史决策、目标、取舍或表达偏好时，按需读取根目录 `个人上下文.md`；普通事实和既有知识检索直接提取问题线索。个人上下文用于入口、取舍和表达，事实依据来自对应的 Wiki / Raw / Routine，个人经历的延伸标为推断。
2. 个人上下文已有准确 Wiki、Raw 或 Routine 入口时直接读取；否则提取 3–8 个名称、日期、术语或完整短语，运行 `scripts/query_search.py <关键词...>`，每个短语作为一个参数。
3. 从搜索结果按子主题、相关性和重要性选择不超过 3 个 Wiki，逐页读取，证据足够时停止。保留候选、精读后排除页和最终证据页的区分。
4. 需要细节、项目验证、来源限制或冲突核对时，只沿 Wiki 直接链接补读 Raw / Routine；不递归追踪或批量确认链接存在性。
5. 依据实际读取内容回答并保留相关证据，不复述来源结构，不展示 `%% agent ... %%`。仅在诊断需要或用户要求时说明候选筛选过程；不推断未确认的个人偏好或计划。
6. 对实际影响回答或承担来源路由的 Wiki 运行 `scripts/update_query_header.py <Wiki路径...>`；候选和排除页不计数。只读请求跳过更新；更新失败只报告维护问题，不阻断回答。

不修改 Wiki 正文、index、个人上下文或维护日志，不自动触发 Ingest、Lint 或 Audit。产生稳定新知识时只提出写入建议。
